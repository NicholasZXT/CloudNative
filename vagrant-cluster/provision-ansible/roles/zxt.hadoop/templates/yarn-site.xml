<?xml version="1.0"?>
<configuration>
{% set dict = hadoop_yarn_site|combine(hadoop_yarn_site_defaults) %}
{% for key,value in dict.items() %}
    <property>
        <name>{{ key }}</name>
        <value>{{ value }}</value>
    </property>
{% endfor %}
</configuration>
