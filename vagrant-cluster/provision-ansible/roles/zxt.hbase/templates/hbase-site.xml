<?xml version="1.0"?>
<configuration>
{% set dict = hbase_site|combine(hbase_site_defaults) %}
{% for key,value in dict.items() %}
  <property>
    <name>{{ key }}</name>
    <value>{{ value }}</value>
  </property>
{% endfor %}
</configuration>
