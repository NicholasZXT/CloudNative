java_home: unknown
spark_version: unknown
spark_local_tar: unknown

spark_user: spark
spark_group: spark
spark_target_dir: /opt/spark
spark_distro_home: "{{ spark_target_dir }}/current"

# 是否删除已有的target_dir
delete_target_dir: false
# 是否重新部署配置项
reconfig: false

# zoo.cfg
# zoo_cfg: {}
# zoo_cfg_defaults:
#   clientPort: 2181

