---
- name: Stop zookeeper server
  shell: "{{ zookeeper_distro_home }}/bin/zkServer.sh stop"
  environment:
    JAVA_HOME: "{{ java_home }}"
  become: yes
  become_user: "{{ zookeeper_user }}"
  ignore_errors: true
